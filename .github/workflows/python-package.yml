name: Python package

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install -e .
      - name: Install test deps
        run: |
          pip install pytest
      - name: Run tests
        run: |
          pytest -q
      - name: End-to-end CLI checks
        run: |
          raccoon --version
          raccoon seq-qc examples/combine/inputs/set_a.fasta examples/combine/inputs/set_b.fasta -o /tmp/combined.fasta
          test -s /tmp/combined.fasta
          raccoon tree-qc --phylogeny examples/mini_tree/mini.pruned.tree \
            --asr-state examples/mini_tree/mini.state \
            --alignment examples/mini_tree/mini.aln.fasta \
            --run-adar
          test -s mini.pruned.phylo_flags.csv
